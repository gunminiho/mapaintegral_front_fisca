const header = () => {
    return {
        'Content-Type': 'application/json', // Tipo de contenido
        //'Authorization': 'Bearer <your-token>', // Si necesitas enviar un token de autorización
        // Otros headers si es necesario
    }
};

export default header;